---
okf_version: "0.2"
status: parked -- the sampler is built and benchmarked; sampling is not the route to the CBM56 result (tpeulen, prompt 474)
---

# PRD-146: the No-U-Turn sampler for the Bayesian decay posterior

**Owner** (tpeulen, 2026-09-15, ucfret prompt 463): "try NUTS." It follows PRD-145, where both samplers
bff already has fail on CBM56:
- the blocked random-walk Metropolis: tau ~13 000 log posteriors per independent draw;
- the ensemble slice sampler: more than 400 000.

**Author:** Claude Opus 5 for tpeulen.

## The algorithm, and where it lives

NUTS as Stan runs it:
- **Trajectory:** Hamiltonian dynamics by leapfrog under a fixed Euclidean metric, doubled forwards
  or backwards at random until the trajectory makes a U-turn (Hoffman & Gelman, JMLR 15, 1593 (2014)).
- **Termination criterion:** Betancourt's generalised one, on the summed momentum `rho`
  ("A Conceptual Introduction to Hamiltonian Monte Carlo", arXiv:1701.02434, appendix A.4.2),
  checked across each subtree merge and its sub-subtrees as Stan does.
- **Sampling:** multinomial sampling of the new state within a subtree, and biased progressive
  sampling between subtrees.
- **Divergence:** declared when the energy error exceeds 1000.
- **Step size:** adapted during warm-up by Nesterov dual averaging to a target acceptance statistic
  of 0.8 (Hoffman & Gelman, Algorithm 5), then frozen.
- **Metric:** dense, the inverse of a given covariance. Here that is the Laplace covariance at the
  best mode, which is the preconditioner the fit already computes.

It is generic (a log density and its gradient in, draws out) and header-only, so it goes to
**tttrlib** (`modules/math/include/NoUTurnSampler.h`, neutral names), vendored into bff and pinned --
the placement rule for simple, header-only algorithms (tpeulen, prompt 448).

## Steps, each with its check

1. **tttrlib `NoUTurnSampler.h`** with tttrlib test `test/cpp/test_no_u_turn_sampler.cpp`.
   - *Done when:*
     - on a 20-d correlated Gaussian (condition 1e4, identity metric) the sample means and variances
       are within 4 MCSE of the truth, with R-hat < 1.01 over 4 chains (`McmcDiagnostics.h`);
     - with the dense metric set to the true covariance, the tree depth drops and the ESS per
       gradient rises (the metric is used);
     - on a funnel-like target with too large a fixed step size, divergences are reported (the check
       can fail);
     - a sampler with the U-turn criterion disabled at a fixed depth of 1 (a plain random walk of one
       leapfrog) gives a far smaller ESS (the control).
   - Vendored into bff and pinned.
2. **bff: the decay posterior's gradient without the information matrix**
   (`bayesian_decay_log_posterior_and_gradient`, in `BayesianDecayPosterior.h`), and
   `bayesian_decay_nuts` running chains on it.
   - *Done when* the gradient equals `bayesian_decay_evaluate`'s to 1e-12 and costs less, and on the
     synthetic experiment, sampling from a start near the mode gives means within 4 MCSE of a long
     reference run with R-hat < 1.01.
3. **CBM56** (ucfret `s89_cpp`), D0 + DA (Rh110 excluded), lambda held first.
   - 4 chains: 2 dispersed around the best mode, 2 started from the second mode's end point.
   - Thresholds declared before the run: R-hat < 1.01, bulk and tail ESS > 400 on mean R/R0, sd R/R0,
     x_d0, g and p(R/R0) at its quartile grid points.
   - Reported: divergences, tree depths, and the time per chain.
   - *Done when* the figure (traces per chain, rank histograms, sampled p(R/R0) against the Laplace
     band) and the log entry exist, and the run either passes, or is excluded and counted with its
     failing summaries named.
   - If lambda-held sampling passes, lambda is freed in a second run.

## Progress

- **Step 1 done, 2026-09-15.** tttrlib 0462f6704 `NoUTurnSampler.h`, vendored and pinned.
  - Tests on a 20-d Gaussian with condition 1e4, 4 chains:
    - means within 2.5 MCSE and variances within 2.9 MCSE of the truth;
    - R-hat 1.005, bulk ESS >= 1013;
    - with the true covariance as metric, the tree depth drops by 3.6 and the ESS per gradient rises 152x;
    - the max-depth-1 control keeps 0.4 % of the ESS;
    - Neal's funnel with a fixed step of 0.8 reports 208 divergences.
  - Found on the way and fixed in `McmcDiagnostics.h`: an unsigned `n - 3` in the Geyer loop read past
    the array with fewer than 4 draws per split chain. It is now pinned by a 5-draw arviz case.
- **Step 2 done, with one finding.**
  - `bayesian_decay_log_posterior_and_gradient` equals `bayesian_decay_evaluate` to 2e-16 of the peak
    (6.2 ms against 7.4 ms on CBM56).
  - `BayesianDecaySampling.h`: `bayesian_decay_nuts_chain` and `bayesian_convergence`.
  - On the synthetic experiment, two independent 4-chain runs agree on every mean (worst 2.1 combined
    MCSE), and a 5-draw run is flagged (R-hat 2.8).
  - They do not converge. At target 0.8, 5-25 % of draws diverge: R-hat 1.06, ESS 88. At target 0.95
    fewer diverge, but R-hat is 1.26 and ESS 12.
  - The divergent draws sit in the response-shift, reference-lifetime and reference-scatter coordinates,
    where this experiment clamps its shifted response at zero on purpose. The posterior has kinks there.
  - Test `test_bayesian_decay_nuts.py`: the convergence check is marked as a strict expected failure.
- **Step 3 first chain (CBM56, D0 + DA, lambda held; 150 warm-up, 150 draws, 843 s).**
  - Sampler: step size 0.011, mean tree depth 8.7, 451 gradients per draw, no divergences after warm-up
    (17 during).
  - Draws: log posterior about 70 below the mode, the typical set of 136 dimensions.
  - Mixing: tau of mean R/R0 1.6 draws, sd R/R0 1.4, g 2.6, x_d0 8.4.
  - Cost per independent draw of mean R/R0: about 4.5 s, against about 29 s for the random-walk
    Metropolis and more for the ensemble slice.
  - ESS 400 on x_d0 would take about 2.6 h for 4 chains at this step size.
  - The small step (a Laplace metric that fits the posterior poorly) is the cost. Stan's windowed
    metric adaptation from warm-up draws is the standard remedy and is not yet implemented.

## Amendment B1 (2026-09-15, tpeulen, ucfret prompt 464): benchmark against Stan before continuing

"before continuing, you must test and benchmark your NUTS against established, ie, stan." Nothing
further on CBM56 until this passes. Reference: CmdStan through cmdstanpy (conda-forge, env `stan`).
Declared before anything is run:

**Targets.** Each is written once as a Stan model and once as a C++ log density with its gradient,
the same function. The C++ gradients are checked against central differences.
- **G20:** a 20-d correlated Gaussian, condition 1e4 (the tttrlib test's).
- **FUNNEL:** Neal's funnel, 10-d, centred.
- **POISSON:** a Poisson regression with 10 coefficients on simulated data (200 counts,
  N(0, 2) priors).
- **SCHOOLS:** the eight-schools model, non-centred.

**Test A: the algorithm, adaptation off.** The same fixed step size and the same metric (identity;
and for G20 also the true covariance, dense) on both sides.
- 4 chains x 4000 draws each, from the same starts.
- *Pass* when:
  1. every coordinate's posterior mean and sd are within 4 combined MCSE of Stan's;
  2. the tree-depth distributions agree (two-sample chi-square on the depth histogram, p > 1e-3);
  3. the mean acceptance statistics differ by < 0.02;
  4. the divergence fractions differ by < 4 binomial sd.
- Nothing here depends on tuning: identical settings must give statistically identical output.

**Test B: step-size adaptation, metric fixed.**
- Stan with `adapt_engaged`, and metric adaptation off (`adapt_metric_window` 0, only the step-size
  phases), against ours with dual averaging. 1000 warm-up, 4000 draws, delta 0.8.
- *Pass* when the adapted step sizes agree within 25 % and the ESS per gradient (bulk ESS of every
  coordinate over total leapfrog steps, minimum over coordinates) agrees within 30 %.

**Test C: benchmark, informational.**
- Stan's full default (diagonal and dense metric adaptation) against ours, reporting ESS per gradient,
  ESS per second and divergences.
- This measures what Stan's metric adaptation buys: the argument for, or against, implementing it next.

**Output.** ucfret `investigation/pinn_pR_anisotropy/s89_cpp/nuts_vs_stan/`: the Stan models, the C++
driver, the Python runner, a table, and a figure with depth histograms and moments per target. Logged
whether it passes or not. A failure blocks the CBM56 runs.

## Amendment B2 (2026-09-15 15:30, Claude for tpeulen, prompt 464): first benchmark results, and the funnel replicate test

**Run 1 found a bug.** Test A failed on G20 (tree-depth chi-square p = 4e-84) and on the funnel.
- Cause: when the trajectory was extended, tttrlib's `NoUTurnSampler` seeded the old trajectory's
  inner-end momentum from the wrong end. It copied `p_fwd_bck` where Stan's `base_nuts` copies
  `p_fwd_fwd`, and `p_bck_fwd` where Stan copies `p_bck_bck`.
- Effect: the cross-subtree U-turn checks tested the wrong momenta, and trees were shallower.
- The analytic tttrlib tests did not catch it, because the moments were still correct.

**Run 2, with the fix.**
- G20, POISSON and SCHOOLS pass A and B, including G20 with the dense metric.
- FUNNEL still fails:
  - test A: depth p 1.5e-27, accept 0.745 vs 0.718, divergences 0.21 % vs 0.48 %;
  - test B: ESS per gradient ratio 6; Stan's minimum bulk ESS is 16.

**Why the funnel test may not be valid.** B1 treats 16000 draws as one sample of the stationary
distribution. With a unit metric the funnel's neck is not reached in 4000 draws (min ESS 16), so
depth, acceptance and divergence depend on where each chain happened to go. A deviation could come
from the implementation or from this non-mixing; B1 cannot tell them apart.

**The replicate test, declared before running.**
- Setup: 10 independent replicates per side (4 chains x 4000 draws, new seeds and new dispersed
  starts per replicate). Test A uses Stan's pilot step size as in B1; test B adapts.
- Per replicate: mean tree depth, mean accept_stat, divergence fraction; for B also the adapted
  step size and log(min bulk ESS per gradient).
- *Pass* for each statistic: |mean_ours - mean_stan| < 4 sqrt(s_ours^2/10 + s_stan^2/10), where
  the s are the between-replicate sds.
- Before trusting the result, the same test is run Stan against Stan (two disjoint seed sets); it
  must pass.
- A failure here counts as an implementation difference and blocks the CBM56 runs, as in B1.
- A regression check goes into the tttrlib test: the fixed-step tree-depth distribution on an iid
  Gaussian, against counts produced by Stan.

## Amendment B3 (2026-09-16 02:30, Claude Opus 5, ucfret prompt 472): the CBM56 chains, thresholds declared before the run

**The run.**
- **Target:** the CBM56 D0+DA posterior (`fixture_cbm56_noref`, dim 136, lambda at the fixture's node).
- **Sampler:** 4 NUTS chains (`NutsKernel`, `adapt_metric: dense`, starting from the Laplace covariance of
  the best declared start); target acceptance 0.8; 1000 warm-up and 1000 draws per chain.
- **Starts:** chains 0 and 2 start 0.5 Laplace sd around the best declared start; chains 1 and 3 around
  the end point of the data start (the second mode found in A4.7).
- **Driver:** ucfret `s89_cpp/cbm56_nuts.cpp`, which writes the draws and a summary.

**Declared thresholds.** A summary may be quoted only if all of these hold:
- rank R-hat < 1.01, bulk ESS > 400 and tail ESS > 400 over the 4 chains, for each of mean R/R0,
  sd R/R0, x_d0, g and the log posterior;
- no divergent transitions after warm-up;
- no transition at the maximum tree depth in more than 1 % of draws.

A failed run is reported with its numbers and excluded, as Rule 0 requires.

**Also reported, whatever the verdict:** per chain the adapted step size, the mean tree depth, the
gradients per draw and the wall time, set against the first (Laplace-metric) chain's step 0.011 and 451
gradients per draw; and whether chains started in different modes agree on the summaries (mean
difference in MCSE).

## Parked (2026-09-16, tpeulen, ucfret prompt 474)

"I already knew that the sampling would not work, this was tried in the past. thus, i went the more
analytical route." / "I do not expect the sampling to pass." The acceptance-0.95 rerun was stopped
mid-flight.

**Delivered and kept:** the sampler (PRD-147's interface, `NutsKernel` with windowed diag/dense metric
adaptation), benchmarked against CmdStan 2.39.0, and the diagnostics checked against arviz.

**Measured on CBM56, and why no sampler is cheap here:** 73 of 136 whitened axes differ from the Laplace
metric at the mode (worst by ~100x), and the curvature along the donor spectrum weights reaches 1e6
within 3 sd. The one multi-chain run passed R-hat and ESS and failed on 238 divergences (B3, excluded).

**If it is ever picked up again:** the open lead is the parameterisation of the donor spectrum, tested on
a simulated twin of the CBM56 design (Rule 0's fit half belongs on simulated data, prompt 473), not on
CBM56 itself.
